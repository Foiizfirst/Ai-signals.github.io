specVersion: "v1beta"
serviceId: "AI Signals Web"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "AI Signals Web-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
